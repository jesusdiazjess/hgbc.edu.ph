"use strict";const toggleElem=function(e){e.classList.toggle("active")},navbar=document.querySelector("[data-navbar]"),navTogglers=document.querySelectorAll("[data-nav-toggler]"),overlay=document.querySelector("[data-overlay]");for(let i=0;i<navTogglers.length;i++)navTogglers[i].addEventListener("click",function(){toggleElem(navbar),toggleElem(overlay)});const header=document.querySelector("[data-header]"),backTopBtn=document.querySelector("[data-back-top-btn]");window.addEventListener("scroll",function(){window.scrollY>=100?(header.classList.add("active"),backTopBtn.classList.add("active"),header.classList.add("header-anim")):(header.classList.remove("active"),backTopBtn.classList.remove("active"),header.classList.remove("header-anim"))});const searchTogglers=document.querySelectorAll("[data-search-toggler]"),searchBox=document.querySelector("[data-search-box]");for(let i=0;i<searchTogglers.length;i++)searchTogglers[i].addEventListener("click",function(){toggleElem(searchBox)});const whishlistBtns=document.querySelectorAll("[data-whish-btn]");for(let i=0;i<whishlistBtns.length;i++)whishlistBtns[i].addEventListener("click",function(){toggleElem(this)});
